<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Estimated_Recurring_Annual_Profit__c</fullName>
    <description>[CDD NE Wave 1] Added as part of solution to track recurring revenue.  (Total profit amount for products where Annual Recurring Product is checked / Contract Length) * 12</description>
    <formula>IF(
   NOT(ISBLANK(Contract_Length_CAT__c)),
   Estimated_Recurring_Total_Profit__c /(Contract_Length_CAT__c/12),
   0
   )</formula>
    <label>Estimated Recurring Annual Profit</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
