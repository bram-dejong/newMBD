<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Opportunity_Monthly_Value__c</fullName>
    <formula>IF(ISBLANK(Amount), NULL,
IF(
   AND(
       NOT(ISNULL(Contract_Length_CAT__c)),
       NOT(ISBLANK(Amount))
      ) ,
    Amount/Contract_Length_CAT__c, Amount
   )
  )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Opportunity Monthly Value</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
